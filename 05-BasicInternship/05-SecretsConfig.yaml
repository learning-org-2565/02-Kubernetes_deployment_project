apiVersion: v1
kind: Secret
metadata:
  name: proshop-secrets
type: Opaque
data:
  # Base64-encoded values for your secrets:
  #   echo -n "mongodb+srv://username:pass@cluster.xyz/..." | base64
  MONGO_URI: <BASE64_ENCODED_MONGO_URI>
  JWT_SECRET: <BASE64_ENCODED_JWT_SECRET>
